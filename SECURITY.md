# 安全策略

## 支持范围

安全修复只保证进入当前最新版本。项目仍处于早期阶段，暂不维护旧版本分支。

## 报告漏洞

请不要在公开 Issue 中披露 API Key、可复现攻击载荷、真实中转站地址或客户数据。

仓库启用 GitHub Private vulnerability reporting 后，请优先通过仓库的 **Security → Report a vulnerability** 私密提交。启用前，请通过维护者公开的私密联系方式报告，并至少包含：

- 受影响版本和运行环境；
- 最小复现步骤；
- 可能造成的影响；
- 已确认不会泄露真实凭据的测试材料。

维护者确认漏洞前，请避免公开传播细节。

## 凭据处理边界

RelayAudit 不持久化 API Key，但运行中的 Key 会经过浏览器、本机 HTTP 服务和目标接口。用户必须自行确认本机环境与目标 Base URL 可信，并且不应把服务直接部署到公网。
