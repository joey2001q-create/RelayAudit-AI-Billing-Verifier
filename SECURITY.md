# 安全策略

## 支持范围

安全修复只保证进入当前最新版本。项目仍处于早期阶段，暂不维护旧版本分支。

## 报告漏洞

请不要在公开 Issue 中披露 API Key、可复现攻击载荷、真实中转站地址或客户数据。

仓库启用 GitHub Private vulnerability reporting 后，请优先通过仓库的 **Security → Report a vulnerability** 私密提交。启用前，请通过维护者公开的私密联系方式报告，并至少包含：

- 受影响版本和运行环境；
- 最小复现步骤；
- 可能造成的影响；
- 已确认不会泄露真实凭据的测试材料。

维护者确认漏洞前，请避免公开传播细节。

## 凭据处理边界

RelayAudit 不持久化 API Key，不主动把 Key 写入日志、返回结果或导出报告。不同运行方式的传输路径如下：

- Vercel 托管版：浏览器 → RelayAudit Vercel Function → 用户填写的目标接口。
- 源码自部署版：浏览器 → 用户自己的 RelayAudit 服务 → 用户填写的目标接口。

托管版只允许访问可公开解析的 HTTPS 地址，并拒绝本机、内网、链路本地和保留地址。自部署版默认只监听 `127.0.0.1`；如自行修改监听地址或部署到公网，凭据保护、访问控制和运行日志由部署者负责。

用户必须自行确认目标 Base URL 可信。不要在 Issue、配置文件、终端记录、截图、导出报告或 `.vercel` 目录中公开真实凭据。
