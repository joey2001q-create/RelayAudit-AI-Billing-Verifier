# 贡献指南

感谢你帮助改进 RelayAudit。提交代码前，请先搜索现有 Issue，避免重复工作。较大的行为变更建议先创建讨论 Issue，明确核验口径、兼容范围和证据边界。

## 本地开发

需要 Node.js 20 或更高版本。

```bash
npm install
npm test
npm run check
npm start
```

本地服务地址为 <http://127.0.0.1:4312>。

## 代码要求

- 保持模块职责单一，优先复用现有解析、计价和归一化模块。
- 不得记录、返回或写入 API Key。
- 新增 usage 兼容格式时，必须增加对应测试。
- 改动计价、请求一致性或证据结构时，必须说明兼容影响。
- “缓存读取占比”不能写成“缓存命中率”。
- 速度与价格核验是不同问题；引入速度测试前应先形成独立设计。
- 界面和面向用户的仓库文档默认使用中文。

## 提交 Pull Request

PR 应包含改动目的、验证命令和必要的兼容说明。请确保：

- `npm test` 通过；
- `npm run check` 通过；
- 不包含真实 API Key、生产 URL、客户名称和导出报告；
- 用户可见术语与 README 的核验口径一致；
- 新行为已补充测试或说明无法自动测试的原因。

提交贡献即表示你同意按项目的 MIT 许可证发布该贡献。
