# 更新日志

本项目的显著变更记录在此文件中，版本格式遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新增

- 新增 Vercel 托管版，用户可以打开网址直接运行计费核验。
- 保留源码自部署版，并与托管版共用同一套测试语料、价格同步、usage 归一化和计费核心。
- 核验 Manifest、文本摘要和 HTML 报告记录本次执行方式。
- 托管版增加公网 HTTPS 地址校验，拒绝本机、内网、链路本地和保留地址。
- README 增加中文、英语、西班牙语和日语版本，并提供顶部语言切换入口。
- 计费结论增加测试模型与完成时间，并在当前浏览器保存最近 20 次脱敏测试历史。

### 调整

- 首屏直接选择 5.6 模型并自动带出模型名和价格。
- 5.6 模型价格改为从公开价格仓库动态同步，不再由前端硬编码。
- 远程价格通过 SHA-256 校验，失败时回退到自动生成的最近有效快照。
- 核验 Manifest、文本摘要与 HTML 报告记录价格来源和文件哈希。
- 计费结果按用户阅读顺序重排：优先展示标称应扣金额、分维度结果和 Token 汇总，实际账单输入改为后续紧凑操作。
- 价格加载改为先启用本地快照、再后台同步远程更新，网络异常不再阻塞计费按钮。
- 修复语料提示元素调整后导致前端初始化中断的问题。
- “高级设置”简化为仅供自定义场景使用的“高级选项”。
- 网站标称倍率移动到第三步，与平台账单金额一并填写。
- 倍率不再进入请求配置和请求证据，只进入计费核验结果。
- 单次测试轮数上限调整为 5，降低托管函数超时和误操作风险。

## [0.1.0] - 2026-08-14

### 新增

- 单平台计费核验和双平台对比模式。
- 单轮稳定性、多轮上下文、缓存复用三种测试维度。
- OpenAI 与 Anthropic 风格 usage 归一化。
- 平台账单金额、标称应扣金额、相对偏差和实测倍率计算。
- 脱敏 JSON 证据、HTML 报告和文本摘要导出。
- cURL、JSON 与三行配置导入。
- 流式展示测试维度、轮次、平台及总体请求进度。
- 高级选项支持自定义测试语料和最大输出 Token。

### 安全

- API Key 不落盘且不进入报告。
- 本地服务仅监听 `127.0.0.1`。
- 失败请求不自动重试。
- 自定义语料正文不进入返回结果和导出报告，只保留摘要信息。
